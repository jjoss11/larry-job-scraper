# FastAPI frontend skeleton

This folder contains a minimal FastAPI app for optionally exposing a simple frontend or API endpoints to view saved job postings and manage scraping schedules/keywords.

To start a real FastAPI app locally, create a virtualenv, install `fastapi` and `uvicorn`, and implement endpoints in `main.py`.
